---

- hosts: all
  become: true
  tasks:

  - name: update cache
    apt:
      update_cache: yes

  - name: teste
    apt:
      name:
        - neofetch
        - btop
        - curl
      state: latest
  
